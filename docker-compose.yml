services:
  fuseki:
    image: stain/jena-fuseki:5.1.0
    ports:
      - "3030:3030"
    environment:
      ADMIN_PASSWORD: admin
      FUSEKI_DATASET_1: kg
    restart: unless-stopped
    volumes:
      - fuseki-data:/fuseki

volumes:
  fuseki-data:
